<?php
use PHPMailer\PHPMailer\PHPMailer;
use PHPMailer\PHPMailer\Exception;

require_once __DIR__ . '/../app/Librerias/PHPMailer/Exception.php';
require_once __DIR__ . '/../app/Librerias/PHPMailer/PHPMailer.php';
require_once __DIR__ . '/../app/Librerias/PHPMailer/SMTP.php';


$mail = new PHPMailer(true);

try {

    // MOSTRAR ERRORES DETALLADOS
    $mail->SMTPDebug = 2;
    $mail->Debugoutput = 'html';

    // CONFIGURACIÓN SMTP
    $mail->isSMTP();
    $mail->Host       = 'sandbox.smtp.mailtrap.io';
    $mail->SMTPAuth   = true;
    $mail->Username   = '1e13c866082a8c';
    $mail->Password   = '735e36197e7af0';

    // TLS recomendado por Mailtrap
    $mail->SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS;
    $mail->Port       = 587;  // PRIMERO PROBAMOS ESTE

    // REMITENTE
    $mail->setFrom('notificaciones@biblioteckj.com', 'BibliotecKJ');

    // DESTINATARIO
    $mail->addAddress('destino@correo.com');  // Cambia por tu correo

    // CONTENIDO
    $mail->isHTML(true);
    $mail->Subject = 'Prueba de Mailtrap';
    $mail->Body    = '<h3>El correo se envío correctamente desde BibliotecKJ</h3>';
    $mail->AltBody = 'El correo se envío correctamente desde BibliotecKJ';

    if ($mail->send()) {
        echo '<h2 style="color:green;">CORREO ENVIADO EXITOSAMENTE</h2>';
    }

} catch (Exception $e) {
    echo "<h2 style='color:red;'>ERROR: {$mail->ErrorInfo}</h2>";
}
